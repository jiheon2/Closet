spring:
#  ai:
#    openai:

  config:
    import: "optional:configserver:http://localhost:8888/"
  cloud:
    config:
      name: style
      profile: prod
    gcp:
      storage:
        credentials:



