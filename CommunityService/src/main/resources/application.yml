spring:
  config:
    import: "optional:configserver:http://localhost:8888/"
  cloud:
    config:
      name: community
      profile: prod

    gcp:
      storage:
        credentials:
          location: classpath:/resources/closetproject-419105-297cdb4ae5b4.json
        bucket: msa-post-bucket
        project-id: closetproject-419105
